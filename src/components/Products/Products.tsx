import ProductsList from "../ProductsList";

const Products = () => {
  return (
    <section className="productsSection">
      <h2>Products</h2>

      <ProductsList />
    </section>
  );
};

export default Products;
